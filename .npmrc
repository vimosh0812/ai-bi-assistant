# Use pnpm for package management
package-manager=pnpm

# Disable strict peer dependencies to avoid conflicts
strict-peer-dependencies=false

# Use legacy peer deps for compatibility
legacy-peer-deps=true
